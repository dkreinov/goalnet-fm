# Plan 20260616-0926 — Roster-constrained matching + grade fill + subs + 2nd lineup source

Plan file: `plans/plan-20260616-0926.md`
Execution mode: **autonomous** (user: "plan it and then execute"; standing autonomous preference)
Model: Opus / thinking budget High
Repo: D:\Programming\claude\FM (Python pipeline, NOT a git repo — no commits; validations only)

## Task summary
Push training-ready coverage from strict-11+11 = 8 full leagues toward ~16-18 by recovering missing starters
WITHOUT risky global name matching. Core idea (user): constrain candidate pool to the club's actual FM squad
for that season → bipartite assignment per match (position + name-similarity + appearance prior). Plus: fill the
1,093 linked-but-no-grade UIDs via targeted per-UID scrape; backfill substitution identities from ESPN cache
(timing exists, player=NULL); add a 2nd lineup source (fuller names) to resolve the residual + cross-validate.

## Baselines to beat
- training-ready strict 11+11: 40,032 / 81,357 (49% all; 57% of target leagues; 8 leagues full×6yr).
- starter→FM coverage 94.6%; test_xwalk 8/8 green (must stay green).
- subs: 495,690 substitution events w/ minute but player=NULL; 57,916 matches have sub events.
- 10→11 work-list: 1,093 linked-but-no-grade UIDs (recoverable) + 7,141 unmatched starters (name floor).

## Frozen contracts
- New table `match_grade_link(match_id, player_id, fm_uid, method, confidence)` — match-level resolution layer,
  consumed by build_dataset AFTER xwalk, BEFORE fallback. Does NOT mutate player_xwalk (global identity stays guarded).
- Roster: squad(club,season) = FM UIDs with a grade snapshot for that club at the season's edition.
- Reuse match_link.py (competition+date±2d+score) for cross-source match alignment.

## Plan Review Summary
- Review passes: 3
- Fixed: Step 2 uses an AUTHENTIC held-out test (hide a known-matched starter, confirm recovery) not just "more links";
  Step 6 feasibility-probe gates the 2nd-source scrape (anti-bot environmental risk); 2nd source ordered AFTER roster
  (cheap-first); match_grade_link kept separate from player_xwalk so authenticity tests stay valid.
- Remaining concern: 2nd-source providers (FBref/Sofascore/Transfermarkt) may be anti-bot blocked in this env → Step 6
  surfaces it; if all blocked, Step 7 adapts/skederps and Steps 1-5 gains still stand.
- Confidence: ready.

---

## Step 1: Build club-season FM squad index + size roster-assignment opportunity (read-only)
Files: create src/diag_roster.py.
Goal: squad(club,season) from player_snapshot⋈club at season edition. For each target match, count unmatched starters
and classify: forced (exactly 1 unmatched + 1 position-compatible unassigned squad member) / position-resolvable /
unresolvable. Output recoverable match count.
Validation: prints matches recoverable by roster-constraint (forced + resolvable) vs unresolvable; forced count > 0.

## Step 2: Implement roster-constrained per-match assignment (matcher)
Files: create src/roster_match.py → writes match_grade_link.
Goal: per match, assign unmatched ESPN starters to unassigned squad members via bipartite assignment scored by
position compatibility + name similarity (token/fuzzy) + appearance prior; confidence tiers (forced=high, scored=medium).
Validation (AUTHENTIC held-out): on matches where all 11 starters are globally matched, hide 1 at random, run matcher
restricted to the club squad, confirm it recovers the correct FM UID in >=85% of forced-single cases. Report accuracy.

## Step 3: Wire roster links into build_dataset + rebuild + verify
Files: src/build_dataset.py (read first).
Goal: resolution order xwalk → match_grade_link → fallback; roster-linked starters count toward n_matched (with a
confidence column so we can down-weight later).
Validation: report_missing strict 11+11 ready > 40,032 (materially); test_xwalk ALL PASS (global xwalk untouched).

## Step 4: Targeted per-UID grade scrape (1,093 linked-but-no-grade)
Files: create src/scrape_uids.py (reuse scrape_fminside.parse_player + batched WAL save; player pages URL-driven by UID — no enumeration).
Validation: grades saved for most of the 1,093; re-query linked-but-no-grade count drops materially; err=0.

## Step 5: Sub-identity backfill from ESPN cache
Files: create src/backfill_subs.py.
Goal: re-parse cached ESPN summary JSON for substitution player-in/player-out + minute; populate match_event.player
(or new match_sub table). Link sub players to player_id/grade the same way as starters.
Validation: substitution events with non-null player rises from ~0 to most of 495,690; spot-check one known match (correct names+minutes).

## Step 6: Second lineup source — feasibility probe + pick
Files: create src/probe_source2.py.
Goal: fetch ONE test fixture's lineup from candidates (FBref, Sofascore, Transfermarkt); pick the one that fetches
reliably here and has fuller/more-formal names than ESPN.
Validation: >=1 source returns a parseable lineup with player names; report chosen source + a naming sample vs ESPN.
Risk: anti-bot/Cloudflare/429 — if ALL blocked, document and skip Step 7 (Steps 1-5 still deliver).

## Step 7: Scrape 2nd-source lineups + align + cross-reference residual
Files: create src/scrape_source2.py (+ integration into roster_match name-bridge).
Goal: scrape lineups for target leagues×seasons from chosen source; align to our matches via match_link
(date+teams+score); use alternate names to resolve residual unmatched starters (feed match_grade_link).
Validation: residual unmatched starters drop; report additional matches recovered. (Conditional on Step 6 success.)

## Step 8: Final rebuild + verify + report
Files: none (build_xwalk, build_dataset --no-flag, test_xwalk, report_missing, status_report).
Goal: rebuild; verify strict 11+11 training-ready rose vs 40,032 and the full-league×6yr count rose vs 8; post tables.
Validation: report_missing strict-11 ready up; test_xwalk ALL PASS; post gap table + full-league×year count to user.
FINAL: after validation passes, rm plans/plan-20260616-0926.md and confirm deletion.

## EXECUTION STATUS (2026-06-16 checkpoint)
- Step 1 DONE — diag_roster.py; found+fixed cross-source club-id split (bridge via matched starters); 27,613 recoverable sides.
- Step 2 DONE — roster_match.py; held-out recovery 96.5% (bar 85%); match_grade_link built (19,026 high / 23,673 medium).
- Step 3 DONE — build_dataset.py wired roster as 4th layer (separate high/medium counts); test_xwalk 8/8 PASS;
  conservative readiness 49%->52% (high-only), 57%->60% target; +medium = 65% target. report_missing uses high-only.
- Step 4 DEFERRED — FM-UID != fminside URL-id, so per-UID scrape infeasible; these are season/league squad gaps. Low ROI.
- Step 5 DONE (running to completion, job bomh9vf7y) — backfill_subs.py; match_sub(in/out player + minute), 100% in-linked.
- Step 6 DONE — 2nd-source probe: FBref 403, Sofascore 403, TRANSFERMARKT 200 (chosen source).
- Step 7 PENDING — full Transfermarkt scraper (match-id map + lineup parse + align + cross-ref residual). Heavy, own session.
- Step 8 PENDING — final rebuild after Step 7.

RESUME: build the Transfermarkt lineup scraper (Step 7). TM is fetchable with a browser UA. Approach: per league-season,
walk TM competition fixtures to collect match URLs, parse XI+subs (player names + TM ids), align to our matches via
match_link (date+teams+score), feed alternate names into roster_match / a name-bridge for the 7,141 residual unmatched.
Then Step 8 rebuild + verify + delete this file.

---
Delete this file (`plans/plan-20260616-0926.md`) ONLY after Step 7 + Step 8 complete. Until then it is the recovery anchor.
